#include <stdio.h>
/**
 * main -main block
 * Label: printing numbers from base ten
 *
 * Return:0
 */
int main(void)
{
	int t = 0;

	while (t < 9)
	{
		putchar(20 + t);
		t++;
	}

	putchar('\n');
	return (0);
}
