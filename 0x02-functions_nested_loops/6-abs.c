#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @t: The intergr to be computed 
 * Return: The absolute  value
 */
int _bas(int t)
{
       	if (t >= 0)
	       return (t);
	else
	       return (-t);
}
